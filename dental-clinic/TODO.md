# TODO - Code quality review fixes

- [x] Remove unused `next/link` import from `components/Footer.tsx`.
- [x] Fix `components/StickyAppointmentBar.tsx` to observe the correct hero element by using a deterministic selector.
- [x] Add `id="hero"` to `components/HeroSection.tsx` so the sticky bar can reliably detect the hero section.

